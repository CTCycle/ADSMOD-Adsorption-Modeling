# ADS-T3-05 rendered browser evidence

- Route: `http://127.0.0.1:5173/public-data/structures`
- Browser: Codex in-app Browser, 1280×720
- Query mode and value: Text / `wurtzite`
- Positive provider result: COD `1011195`, Zinc sulfide, coordinates available.
- The browser Import action opened the persisted local detail view with cell metrics and two normalized atomic coordinates.
- After the explicit API association to the disposable isolated ZnS fixture, a route reload showed `Zinc sulfide (validation fixture)` in the Material column.
- A repeat browser import retained that material association; the persisted Structures table remained one record for COD `1011195`.

The rendered search results, linked local row, and detail panel were inspected at the stated viewport. Screenshot captures were displayed inline during the task; a workspace screenshot file could not be exported under the browser URL policy.
